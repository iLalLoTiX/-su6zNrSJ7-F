// The file contents for the current environment will overwrite these during build.
// The build system defaults to the dev environment which uses `environment.ts`, but if you do
// `ng build --env=prod` then `environment.prod.ts` will be used instead. http://localhost:3000/api
// The list of which env maps to which file can be found in `.angular-cli.json` https://famarg-api.herokuapp.com/api.

export const environment = {
  production: true,
  base_url:'http://192.168.1.120:3000/api'
};
